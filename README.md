# `isochronic-vagrant-valhalla`

VM for Valhalla

### Notes

To start, run `valhalla_service valhalla.json 1`, `1` sets the concurrency.

Also, remember to set `isochrone.max_contours` to `60` in `valhalla.json`.

